"""Streamlit user-interface layer.

Every module here is presentation only. Business rules, permissions and
persistence stay in ``app.services``, ``app.auth`` and the domain modules; the
UI merely renders what those layers already permit.
"""
